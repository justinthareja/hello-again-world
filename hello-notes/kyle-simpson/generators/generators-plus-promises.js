// this is the new baseline

// yield out a promise then
// resume a generator
// in a circle

// async function foo() {
//     await ajax(...);
// }

// foo(); // returns a promise that gets resolved when the async function completes